PORT=3000
WORKER_ID=1

DATABASE_URL=postgres://shortener:shortener@localhost:5432/url_shortener
REDIS_URL=redis://localhost:6379

# Cache TTL for short→long mappings (seconds). 0 = no expiry.
CACHE_TTL_SECONDS=86400

# Public base used when returning shortened URLs
BASE_URL=http://localhost:8081
